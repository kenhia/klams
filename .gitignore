# Rust / Cargo
/target/
**/target/
**/*.rs.bk
*.pdb
Cargo.lock.bak

# Local dev artifacts
.scratch/
.scratch-agent/

# Editor / tooling
.vscode/mcp.json

# OS
.DS_Store
Thumbs.db

# Viewport (Tauri + SvelteKit + cargo-xwin)
viewport/xwin/
viewport/build/
viewport/node_modules/
viewport/.svelte-kit/
viewport/src-tauri/target/
viewport/src-tauri/gen/

# Deploy / runtime config (rendered, never committed)
deploy/compose.env

# Sprint 006 — backup artifacts and sizing capture.
# backup_dir is configured outside the repo (typically a NAS mount),
# but local dev/test runs can spill into the workspace; ignore both.
/backups/
**/*.dump
**/*.dump.partial
**/*.snapshot
**/*.snapshot.partial
specs/006-maintenance-and-backups/sizing.md
specs/006-maintenance-and-backups/dashboard-smoke.md

# Storage root ($KLAMS_ROOT, default /ai/klams/) lives outside the repo.

# Common local/runtime noise
*.log
.env
.env.*
*.tmp
*.swp
.idea/
