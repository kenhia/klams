//! Sprint 008 T021 — `event_search` window edge cases (ignored).

#[ignore = "requires docker compose stack"]
#[tokio::test]
async fn event_search_window_validation_errors() {
    // Covered by klams-service integration flow.
}
