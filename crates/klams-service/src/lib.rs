//! klams-service crate library entry.
//!
//! Exposes the runtime modules used by both the binary entrypoint
//! (`src/main.rs`) and the integration test suite under `tests/`.

pub mod backup;
pub mod config;
pub mod logging;
